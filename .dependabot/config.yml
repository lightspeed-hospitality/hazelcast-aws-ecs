# Config file documentation: https://dependabot.com/docs/config-file/
version: 1
update_configs:
  - package_manager: "java:maven"
    directory: "/"
    # Security updates are always created as soon as possible (i.e., "live").
    update_schedule: "daily"
    allowed_updates:
      - match:
          update_type: "security"
